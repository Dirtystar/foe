"""Browser Automation Platform — a generic, site-agnostic visual browser
automation runtime (ports & adapters).

`__version__` is the single source of truth for the package version, surfaced
by the CLIs (`bap --version`, `bap-run --version`) and kept in step with
pyproject's `[project].version`.
"""

from __future__ import annotations

__version__ = "0.1.0"

__all__ = ["__version__"]
