# Vision Validation Report — current health

_Generated by Milestone 4.11 — the observe-only Vision Validation suite. Press
**Validate Vision** in the desktop app (Validation page) to reproduce this
against a live World; this committed copy was generated **offline** against a real
reviewed Forge frame (`review_batch_002/frame_000614.png`, 1920×1080, calibrated
ROIs) because this environment has no live browser. It changes no behaviour — it
runs the existing pipeline and grades every stage._

Generated: `2026-08-03T12:48:47+00:00`

## How to read this
Each section is graded **PASS / WARNING / FAIL / INFO**. WARNING/FAIL rows carry a
probable reason and a recommended operator action (e.g. "Run Set Weakening
Region", "Collect more reviewed live frames"). Overall status is the worst
section. The suite is **observe-only**: it computes and draws a would-click point
but never clicks, moves the cursor, or types.

## Notes on this sample frame
- The frame is a hard active-learning negative-leaning battle frame: 6 badges
  detected, four classified `20%`, two safely UNKNOWN — matching `RETRAIN_REPORT.md`.
- **Weakening** reads with low confidence here (→ UNKNOWN, fail-safe) — the sample
  ROI is calibrated but the number is not confidently legible on this frame; the
  gate correctly returns UNKNOWN (no action).
- **Battle-map geometry** shows a small `1px overhang` WARNING: `review_batch_002`'s
  battle-map calibration is 1px taller than the 1080px frame. The pipeline clamps
  crops, so it is harmless; the validator flags it with a "recalibrate" action.
  This is a **data/calibration** note, not a code defect (unchanged this milestone).

---

# Vision Validation — WARNING

- World: **H (sample frame)**  ·  source: offline frame  ·  `2026-08-03T12:48:42+00:00`
- Checks: ✅ 6 PASS · ⚠️ 3 WARNING · ❌ 0 FAIL · ℹ️ 35 INFO

## ✅ Capture — PASS

_The one raw capture the whole pipeline analyses._

| check | status | value | note |
|---|---|---|---|
| capture successful | ✅ PASS | yes | A single raw capture is the input to every downstream stage. |
| resolution | ℹ️ INFO | 1920×1080 | Pixel size of the captured content viewport. |
| viewport | ℹ️ INFO | (not reported) | Browser CSS viewport; only reported by a live CDP capture. |
| DPR | ℹ️ INFO | (not reported) | Device-pixel-ratio; affects calibration keying. |
| zoom | ℹ️ INFO | (not reported) | Page zoom; a zoom change invalidates a calibrated ROI. |
| capture latency | ℹ️ INFO | (offline) | Time to obtain the capture; measured only for live captures. |

## ⚠️ Weakening — WARNING

_The per-World safety gate that decides whether any target is actionable._

| check | status | value | note |
|---|---|---|---|
| ROI present | ✅ PASS | yes | The weakening ROI is the safety gate's only input. |
| ROI calibrated | ✅ PASS | yes | A calibrated ROI is keyed to this exact capture geometry. |
| OCR confidence | ⚠️ WARNING | 0.00 | Confidence vs the gate's minimum 0.60. |
| human-readable value | ℹ️ INFO | 2 | The number the reader extracted (operator should confirm it matches the screen). |
| history consistency | ℹ️ INFO | single frame | No prior reads for this World in this run — nothing to compare yet. |
| gate result | ⚠️ WARNING | UNKNOWN | UNKNOWN → no action; ≥ limit → STOP; confident below-limit → CONTINUE. |

- _OCR confidence_ — probable reason: read below the confidence bar · **action: Re-capture; recalibrate the ROI if it persists.**
- _gate result_ — probable reason: weakening unreadable → fail-safe UNKNOWN · **action: Calibrate the ROI so the gate can read.**

## ⚠️ Battle Map — WARNING

_Where badges are detected — the whole usable battleground._

| check | status | value | note |
|---|---|---|---|
| battle ROI | ℹ️ INFO | (430,506,1490,575) | The analysed map region, in full-capture pixels. |
| size | ℹ️ INFO | 1490×575 | Width × height of the map ROI. |
| coverage | ✅ PASS | 41% of frame | The ROI should cover the usable map, not a sub-rectangle. |
| geometry validity | ⚠️ WARNING | 1px overhang | The ROI extends a few px past the edge; the pipeline clamps it. |
| coordinate mapping | ✅ PASS | full-image | Detector boxes are mapped back to full-capture pixels (ROI offset applied once). |

- _geometry validity_ — probable reason: calibration slightly larger than the capture (harmless — crops are clamped) · **action: Recalibrate the battle-map region to remove the overhang.**

## ✅ Badge Detection — PASS

_Locating weakened-sector badges on the map._

| check | status | value | note |
|---|---|---|---|
| candidate count | ℹ️ INFO | 50 | Stage-1 colour-prior candidates before template confirmation. |
| accepted count | ℹ️ INFO | 6 | Candidates confirmed as badges after template + NMS. |
| rejected count | ℹ️ INFO | 44 | Candidates dropped (below template threshold or NMS-suppressed). |
| false panel detection | ✅ PASS | none | Guards against drawing a province-panel overlay on the map. |
| per-stage timing (detection) | ℹ️ INFO | 5262 ms | Detection dominates the tick; measurement only. |

## ℹ️ Classification — INFO

_Reading each badge's percentage — accepted only above the similarity bar._

| check | status | value | note |
|---|---|---|---|
| 20% | ℹ️ INFO | 4 | Classified badges at this percentage. |
| 40% | ℹ️ INFO | 0 | Classified badges at this percentage. |
| 60% | ℹ️ INFO | 0 | Classified badges at this percentage. |
| 80% | ℹ️ INFO | 0 | No labelled exemplars exist for this class yet. |
| 100% | ℹ️ INFO | 0 | Classified badges at this percentage. |
| UNKNOWN | ℹ️ INFO | 2 | Badges whose percentage stayed UNKNOWN (fail-safe, never guessed). |
| confidence histogram | ℹ️ INFO | [0.00-0.50]2 [0.50-0.70]0 [0.70-0.85]1 [0.85-1.01]3 | Nearest-exemplar similarity buckets; accept bar = 0.70. |
| nearest exemplar similarities | ℹ️ INFO | 0.88, 0.86, 0.86, 0.83, 0.25, 0.25 | Top nearest-exemplar cosine similarities across candidates. |

## ℹ️ Decision — INFO

_The deterministic, observe-only target choice and its safety gating._

| check | status | value | note |
|---|---|---|---|
| decision | ℹ️ INFO | UNKNOWN | The per-World gate outcome for this frame. |
| selected badge | ℹ️ INFO | 20% @ (937,772) | The badge a deterministic strategy would engage (lowest allowed %, then confidence, then nearest). |
| ignored badges | ℹ️ INFO | 2 | Badges skipped (unknown % or disabled for this World), with recorded reasons. |
| decision explanation | ℹ️ INFO | No world settings — lowest weakening with highest confidence. | Human-readable reason for the selection. |
| would-click point | ℹ️ INFO | (937,772) | The point a strategy WOULD click — computed, drawn, never performed (observe-only). |
| gate status | ℹ️ INFO | blocked by gate | Whether the would-click is gate-actionable. Nothing is ever clicked. |

## ℹ️ Performance — INFO

_Per-stage timing of this validation (measurement only — no optimisation)._

| check | status | value | note |
|---|---|---|---|
| capture | ℹ️ INFO | 0 ms | Frame decode / obtain. |
| detector | ℹ️ INFO | 5262 ms | Badge localization (dominant cost). |
| classifier | ℹ️ INFO | 110 ms | Percentage classification + panel check. |
| OCR | ℹ️ INFO | 208 ms | Weakening reader. |
| decision | ℹ️ INFO | 0 ms | Gate + target selection. |
| total | ℹ️ INFO | 5581 ms | Whole validation tick. |
| CPU | ℹ️ INFO | 340% | Process CPU over the validation window. |
| RAM | ℹ️ INFO | 79 MB | Process resident memory. |
